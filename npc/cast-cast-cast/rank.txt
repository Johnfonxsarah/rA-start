mid_camp,219,292,6	script	ป้าย#RANK	857,{
	query_sql("SELECT `char`.`name`, `char`.zeny FROM `char` ORDER BY `char`.zeny DESC",.@n$,.@z);
	mes "จัดอันดับ";
	for(.@i = 0; .@i < getarraysize(.@n$); .@i++)
	mes (.@i + 1) + ".) " + .@n$[.@i] + " (" + callfunc("KM",.@z[.@i]) + ")";
	close;

OnInit:
	waitingroom strnpcinfo(2),0;
	end;
}
