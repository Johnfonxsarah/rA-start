bl_depth2,113,72,5	script	เครื่องเสริมแกร่ง	2001,{
	menu
	"Body",L_Body,
	"Trait",L_Trait,
	"Skill Level",L_SkillLevel,
	"แลกแต้มเป็น Item",L_ExchangePoints,
	"แลก Item เป็นแต้ม",L_ExchangeItems
	;
	close;
	
	L_Body:
	close;
	
	L_Trait:
	close;
	
	L_SkillLevel:
	close;
	
	L_ExchangePoints:
	.@index = select("แต้ม Body:แต้ม Trait:แต้ม Skill");
	close;
	
	L_ExchangeItems:
	.@index = select("แต้ม Body:แต้ม Trait:แต้ม Skill");
	close;
	
OnInit:
	waitingroom "เพิ่ม Status, Trait และ Skill Level",0;
	end;
}
